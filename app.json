{
  "name": "drumpf",
  "scripts": {
  },
  "env": {
    "BOT_ID": {
      "required": true
    },
    "BOT_NAME": {
      "required": true
    },
    "SLACK_BOT_TOKEN": {
      "required": true
    },
    "WEBHOOK_URL": {
      "required": true
    },
    "FLASK_SECRET_KEY": {
      "description": "A secret key for verifying the integrity of signed cookies.",
      "generator": "secret"
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
